import React, {useState} from "react";
function Updater(){
    const [count, setCount] = useState;

    function increment(){
        setCount(c => c + 1);
        setCount(c => c + 1);
        setCount(c => c + 1);

    }
}
export default Updater